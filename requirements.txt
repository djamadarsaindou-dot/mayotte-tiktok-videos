groq>=0.11.0
google-generativeai>=0.8.3
edge-tts>=6.1.12
requests>=2.32.0
Pillow>=10.4.0
python-dotenv>=1.0.1

# Coqui XTTS v2 (voix humaine, optionnel — TTS_PROVIDER=coqui)
# Note : torch est installé séparément via index PyTorch dans le workflow
coqui-tts[codec]>=0.27.0
transformers>=4.57

